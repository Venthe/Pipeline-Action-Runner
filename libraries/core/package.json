{
  "name": "@pipeline/core",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "tsc": "tsc",
    "jest": "jest",
    "test": "jest --config=./jest.config.ts",
    "clean": "(mkdir -p dist || true) && rm -rf dist/*",
    "build": "tsc --project tsconfig.json",
    "build:watch": "npm run --silent build -- --watch"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@pipeline/process": "*",
    "process": "^0.11.10"
  },
  "devDependencies": {
    "@pipeline/types": "*"
  }
}
